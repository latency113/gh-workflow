name: first-Workflow
on: workflow_dispatch
jobs:
  first_job:
      runs-on: ubuntu-latest
      steps:
        - name: lscpu 
          run: lscpu
        - name: Good Bye
          run: echo "Goodbye Docker 👍"
